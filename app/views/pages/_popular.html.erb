<% @categories.each do |category|%>
  <div class="category">
    <img src = "http://placekitten.com/g/370/150" />
    <h4><%= link_to category.name, category_path(category) %></h4>
    <p><%= category.description %></p>
  </div>
<% end %> 