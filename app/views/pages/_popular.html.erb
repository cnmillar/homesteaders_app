<% @popular.each do |pop| %>
  <div class="category" data-equalizer-watch>
    <img src = <%= pop.image %> />
    <h4><%= link_to pop.name, category_path(pop) %></h4>
    <p><%= pop.description %></p>
  </div>
<% end %> 